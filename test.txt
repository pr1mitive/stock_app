// 1. Utils が読み込まれているか確認
console.log('Utils:', window.INVENTORY_UTILS);

// 2. 日付フォーマットのテスト
const today = INVENTORY_UTILS.getToday('YYYY-MM-DD');
console.log('Today:', today);

const formatted = INVENTORY_UTILS.formatDate(new Date(), 'YYYYMMDD');
console.log('Formatted:', formatted);

// 3. 7日後の日付
const future = INVENTORY_UTILS.addDays(new Date(), 7);
console.log('7 days later:', INVENTORY_UTILS.formatDate(future));

// 4. ゼロ埋めテスト
console.log('padZero(5, 3):', INVENTORY_UTILS.padZero(5, 3)); // "005"

// 5. 小数点丸めテスト
console.log('roundDecimal(1.234, 2):', INVENTORY_UTILS.roundDecimal(1.234, 2)); // 1.23

// 6. HTMLエスケープテスト
console.log('escapeHtml:', INVENTORY_UTILS.escapeHtml('<script>alert("test")</script>'));

// 7. バリデーションテスト
console.log('isRequired(""):', INVENTORY_UTILS.isRequired('')); // false
console.log('isRequired("ABC"):', INVENTORY_UTILS.isRequired('ABC')); // true
console.log('isNumber(123):', INVENTORY_UTILS.isNumber(123)); // true
console.log('isPositive(-5):', INVENTORY_UTILS.isPositive(-5)); // false

// 8. アラート表示テスト
INVENTORY_UTILS.showAlert('テスト成功！', 'success');

// 9. ローディング表示テスト（3秒後に自動非表示）
INVENTORY_UTILS.showLoading();
setTimeout(() => {
  INVENTORY_UTILS.hideLoading();
}, 3000);
